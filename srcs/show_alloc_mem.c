#include "ft_malloc.h"

void		show_alloc_mem(void)
{
	/*t_block		*block;

	block = get_first_block(0);
	while (block)
	{
		printf("%p - %p : %lu\n", block, block + block->size, block->size);
		block = block->next;
	}*/
}
